services:
  lexi-geek-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lexi-geek-backend
    ports:
      - "8000:8000"
    networks:
      - shared-network

networks:
  shared-network:
    external: true
    name: shared-network
